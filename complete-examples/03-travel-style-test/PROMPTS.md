# 여행 스타일 테스트용 Codex 프롬프트

오전에 만든 HTML의 문구를 이 폴더 `src/content`에 반영해요. 앱을 처음부터 다시 만들지 마세요.

`src/content/quiz.ts`의 질문과 선택지 문구를 바꾸되, UI에서 선택 가능한 점수로 explorer·balance·planner 세 결과 범위가 모두 도달 가능하게 유지해 주세요. 먼저 변경할 파일을 말하고, 완료 후 `pnpm check` 결과를 요약해 주세요.

콘텐츠는 `src/content`에서 편집하고, 표시 이름과 대표 색상은 `pnpm setup` 및 `service.config.json`에서 바꾸며, `src/platform`은 일반 실습에서 수정하지 마세요.
