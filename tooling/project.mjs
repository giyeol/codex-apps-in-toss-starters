/**
 * 수업에서 전원이 여는 연습 폴더 하나의 설정이에요.
 * `tooling/base`를 이 값으로 렌더링해 `app/`에 생성해요.
 */
export const project = {
  id: "app",
  outputDirectory: "app",
  packageName: "gift-draw-app",
  displayName: "선물 뽑기",
  demoAppName: "course-gift-draw",
  feature: "giftDraw",
  primaryColor: "#FF6000",
  tagline: "상자를 열면 금액이 쌓여요",
};

export const projects = [project];
