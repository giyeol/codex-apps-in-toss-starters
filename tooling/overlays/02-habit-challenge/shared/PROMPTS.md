# 7일 습관 챌린지용 Codex 프롬프트

`src/content/habit.ts`의 습관명과 응원 문구를 바꾸되 오늘 기록과 최근 7일 흐름은 유지해 주세요. 먼저 변경할 파일을 말하고, 완료 후 `pnpm check` 결과를 요약해 주세요.

콘텐츠는 `src/content`에서 편집하고, 표시 이름과 대표 색상은 `pnpm setup` 및 `service.config.json`에서 바꾸며, `src/platform`은 일반 실습에서 수정하지 마세요.
